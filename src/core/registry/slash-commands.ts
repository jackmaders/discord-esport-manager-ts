import pingCommand from "../../modules/ping/commands/ping.command";

/**
 * Commands array containing all slash commands to be registered.
 */
const slashCommands = [pingCommand];

export default slashCommands;
